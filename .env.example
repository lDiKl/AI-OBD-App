# ============================================================
# This file is for DOCUMENTATION only — do not fill in real values here.
#
# To get started:
#   1. cp backend/.env.example  backend/.env       ← API secrets
#   2. cp web/.env.local.example web/.env.local    ← Firebase Web config
#   3. docker-compose up --build
#
# Open:
#   Web app   → http://localhost:5173
#   API docs  → http://localhost:8000/docs
# ============================================================
